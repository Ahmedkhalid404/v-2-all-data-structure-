#include<stdio.h>
#include "global.h"
int main()
{
    List l;
    creaeteList(&l);
    insert(&l,89,0);
    insert(&l,56,0);
    insert(&l,56,0);
    insert(&l,33,0);
    insert(&l,1,0);
    insert(&l,0,0);
    insert(&l,666,0);
    insert(&l,-4,0);
    insert(&l,9999,5);

    return 0;
}









